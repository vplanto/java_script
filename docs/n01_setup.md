# Налаштування середовища (Environment Setup)

Ви не зможете будувати хмарочос, маючи лише молоток. Вам потрібен професійний інструментарій.
Це **обов'язковий набір** для проходження курсу.

---

## 1. Редактор коду: VS Code

Ми не пишемо код у Word або Notepad. Ми використовуємо IDE.
**Visual Studio Code** — це стандарт індустрії.

1.  **Скачати:** [code.visualstudio.com](https://code.visualstudio.com/)
2.  **Встановити розширення (Extensions):**
    * **Live Server** (Ritwick Dey) — щоб запускати локальний сервер в один клік.
    * **Prettier** — щоб ваш код виглядав охайно автоматично.
    * **ESLint** — щоб бачити помилки до того, як вони зламають гру.

---

## 2. Контроль версій: Git

Це ваша "машина часу". Без Git ви втратите код.

1.  **Скачати:** [git-scm.com](https://git-scm.com/)
2.  **Перевірка:** Відкрийте термінал і напишіть `git --version`.
3.  **Конфігурація (зробити один раз):**
    ```bash
    git config --global user.name "Ваше Ім'я"
    git config --global user.email "ваш@email.com"
    ```

---

## 3. Runtime: Node.js

Навіть якщо ми вчимо фронтенд, Node.js потрібен для інструментів розробки.

1.  **Скачати (LTS версію):** [nodejs.org](https://nodejs.org/)
2.  **Перевірка:** `node -v` та `npm -v`.

---

## 4. Браузер: Chrome

Не Safari, не Edge. Для розробки ми використовуємо **Google Chrome**.
Чому? Тому що його **DevTools** (F12) — це найпотужніший інструмент налагодження у світі.

---

## 🛠 Як працювати з цим курсом

1.  Клонуйте свій репозиторій: `git clone https://github.com/ваш-нік/ваш-проєкт.git`
2.  Відкрийте папку у VS Code (`File -> Open Folder`).
3.  Створіть `index.html`.
4.  Натисніть кнопку **"Go Live"** (справа внизу), щоб запустити сервер.