# Git: Ваша машина часу

Інженер не носить код на флешці. Інженер не відправляє архіви `kursova_final_v2_new.zip` в Telegram.
Інженер використовує **Git**.

Це система контролю версій. Вона дозволяє вам:
1.  Зберігати історію змін ("повернути все як було вчора").
2.  Синхронізувати код між домом і університетом.
3.  Публікувати сайт в інтернет (GitHub Pages).

---

## 1. Базовий цикл (Мантра)

Ви будете повторювати ці 3 команди кожен раз, коли зробили щось корисне.

### Крок 1: `git add .` (Покласти в коробку)
Ви кажете: "Я хочу зберегти всі зміни, які я зробив".
```bash
git add .
```

### Крок 2: `git commit -m "..."` (Заклеїти коробку)

Ви створюєте "знімок" (Save Point). Обов'язково пишіть зрозумілий коментар\!

```bash
git commit -m "Додав логіку гри"
# або
git commit -m "Виправив баг з кнопкою"
```

### Крок 3: `git push` (Відправити на склад)

Ви відправляєте ваші зміни на сервер (GitHub).

```bash
git push
```

-----

## 2\. Як почати (Перший раз)

1.  Створіть **новий репозиторій** на сайті [github.com](https://github.com/new).
      * Назвіть його `web-engineering-course`.
      * Поставте галочку "Add a README file".
2.  Натисніть зелену кнопку **Code**, скопіюйте посилання (`https://github.com/...`).
3.  Відкрийте термінал (VS Code -\> Terminal -\> New Terminal).
4.  Напишіть:

    ```bash
    git clone https://github.com/ВАШ_НІК/web-engineering-course.git
    ```
5.  Зайдіть у папку:

    ```bash
    cd web-engineering-course
    ```
6.  Відкрийте цю папку в редакторі:

    ```bash
    code .
    ```

-----

## 3\. GitHub Pages: Ваш сайт в Інтернеті

Як зробити так, щоб вашу гру побачив друг?

1.  Запуште код на GitHub (`add` -\> `commit` -\> `push`).
2.  Зайдіть у налаштування репозиторію на сайті: **Settings** -\> **Pages**.
3.  У пункті **Branch** виберіть `main` і папку `/` (root). Натисніть **Save**.
4.  Зачекайте 2 хвилини. Зверху з'явиться посилання вигляду:
    `https://ваш-нік.github.io/web-engineering-course/`

Це посилання ви здаєте викладачу.

-----

## 🚨 SOS: Щось пішло не так

### "Я щось натиснув і все зникло\!"

Поки ви не зробили `commit`, зміни можна скасувати:

```bash
git checkout .
```

(Це поверне файли до стану останнього збереження. Обержено\!)

### "Git свариться і каже error: failed to push some refs"

Це означає, що на сервері є зміни, яких немає у вас (наприклад, ви редагували файл через сайт).
Треба спочатку забрати їх:

```bash
git pull
```

А потім знову `git push`.